# 포스팅 작성 방법

## 파일과 게시 날짜

글은 `_posts/yyyy-MM-dd-이름.md`에 작성합니다. 파일 맨 위에는 YAML front matter를 넣습니다.

```yaml
---
title: "그래프 탐색에서 방문 처리가 중요한 이유"
date: 2026-09-15 12:00:00
categories:
  - Algorithm
  - C++
tags:
  - BFS
  - Graph
---
```

`title`은 제목, `date`는 Jekyll이 사용하는 게시 날짜입니다. 실제 공연일이나 사건 날짜는 본문에 별도로 적을 수 있습니다. 글의 기본 레이아웃은 `_config.yml`에서 `post`로 지정되어 있습니다.

파일명 날짜와 front matter 날짜가 다르면 게시 날짜와 생성 URL은 front matter의 영향을 받습니다. 기존 파일명이나 날짜를 단순히 맞추려 바꾸지 않습니다.

현재 사이트 시간대는 `GMT-5`입니다. 오프셋 없는 날짜를 한국 시간으로 단정하지 않으며, 시간대 설정 변경은 별도 작업으로 다룹니다.

## 카테고리와 태그

`categories`와 `tags`는 배열로 작성합니다. 현재 `permalink: pretty` 설정에서 여러 카테고리와 게시 날짜는 URL에 반영됩니다. 기존 글의 카테고리 변경은 링크 변경으로 이어질 수 있으므로 먼저 생성 URL을 확인합니다.

- 기존 공개 글은 로컬 아카이브에 보관했습니다. 새 분류는 실제 새 글을 작성할 때 정하며, 빈 카테고리를 미리 만들지 않습니다.
- 공연 글의 태그는 `내한`, `원정`만 사용합니다. 두 유형을 함께 다룬 글에는 둘 다 붙일 수 있으며 아티스트별 태그는 사용하지 않습니다.
- 개발·음악은 기록의 소재입니다. 미래의 내가 다시 떠올리고 싶은 장면과 감정을 중심으로 쓰되, 모든 글에서 교훈을 끌어낼 필요는 없습니다.

## 내부 링크

다른 게시 글을 연결할 때 `/_posts/파일명.md`를 URL로 쓰지 않습니다. Jekyll의 `post_url`에 **확장자를 뺀 실제 파일명 전체**를 넣습니다.

{% raw %}
```liquid
[관련 글]({% post_url yyyy-MM-dd-실제파일명 %})
```
{% endraw %}

위 예시는 문법 설명이며 실제 공개 글의 파일명으로 바꿔야 합니다. 여기서 날짜는 대상 파일명 기준입니다. Jekyll이 대상 글의 게시 날짜·카테고리를 반영해 URL을 생성합니다. 비공개 보관 글은 링크 대상으로 사용할 수 없습니다.

## 이미지와 본문

사진은 `post_image/`의 관련 폴더에 두고 `/post_image/폴더/실제파일명.jpg` 형식으로 참조합니다. 대소문자와 확장자를 실제 파일과 일치시키고, 대체 텍스트에는 사진 내용을 적습니다. 개인 정보·타인의 얼굴·티켓 식별 정보가 노출되지 않는지도 확인합니다.

- 기술 글: 문제 또는 개념 → 접근 방법 → 코드 → 시간복잡도 → 주의할 입력 순서로 구성할 수 있습니다. 코드는 `cpp` 코드 블록에 작성하고 예제·경계 입력으로 검증합니다.
- 공연 후기: 공연 정보 → 준비·이동 → 현장 경험 → 감상 순서로 구성할 수 있습니다. 실제 경험과 추가 확인이 필요한 정보를 구분합니다.

기존 보관 글은 작성 재료라는 이유만으로 이동하거나 공개하지 않습니다.

## 로컬 확인

저장소 디렉터리에서 실행합니다.

```sh
bundle check
# 의존성이 없을 때만 실행
bundle install
bundle exec jekyll build
bundle exec jekyll serve
```

생성 결과는 `_site/`에 있습니다. 미리보기 주소는 터미널 출력을 따릅니다. 미래 날짜의 글은 기본 미리보기에 표시되지 않을 수 있습니다. 미래 글 확인이 필요하면 `bundle exec jekyll serve --future`를 사용하되, 표시를 위해 기존 게시 날짜를 변경하지 않습니다.

발행 전 확인 목록:

- 제목·게시 날짜·카테고리·태그가 의도와 일치하는가?
- 홈·아카이브·태그에서 글을 찾을 수 있는가?
- 내부 링크가 실제 게시 페이지로 열리는가?
- 사진과 코드가 데스크톱·모바일 화면에서 읽히는가?
- 기존 글의 URL이나 보관 글의 상태가 의도치 않게 바뀌지 않았는가?
- `git diff --check`와 `git diff --name-only`로 오류와 변경 범위를 확인했는가?

로컬 빌드·미리보기는 배포가 아닙니다. commit/push와 원격 배포는 별도 승인·절차로 다룹니다.
